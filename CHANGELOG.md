# 更新日志

本文件记录 Shigure for macOS 的用户可见变化。

## 未发布

- 修复吸奶盾美德释放时点和最后 GCD 抢占：可读取真实施法时以匹配记录的 `cast.endsAt + 2 秒`进入阶段 3，不再受迟到终态回调影响；真实时点不可用时保留 DiGua 姓名板 `11.7 + 2 秒`回退。类型 2/阶段 5 进入时会作废既有普通 GCD 待发送动作，并由运行时独立于模块规则顺序统一拦截新的普通 GCD；圣疗术、非 GCD、驱散和受伤友方 NPC 治疗例外。
- 修复 DiGua 更新后因版本号硬编码导致桥接停用的问题：改为探测关键时间轴 API 能力，兼容仍提供相同接口的后续 DiGua 版本；缺少关键 API 时继续安全停用，并保留检测到的版本用于诊断。

- 奶骑模块升级至 `1.2.1.22`（41 条规则）：新增非玩家队友牺牲祝福、治疗石优先于浓缩银月城生命药水并受圣盾术门禁、正义盾击距离校验（移除副本内不可用的正面条件），以及 AOE 日志中的“鸣钟预计可用”诊断字段；无库存物品现在明确按不可用处理。

- 修复动作确认串线和并发发送：所有带动作技能码的技能统一进入单飞行确认窗口，普通 GCD 动作在前一动作确认前不得换目标或抢发；只有明确非 GCD 技能可抢占，旧确认代次会被丢弃，迟到事件不会再消费新动作。
- 收紧奶骑动作确认：同一技能在待确认或成功后的短保护窗口内不得换目标、重复发送或被冷却滞后状态再次触发；首次明确失败使用短重试退避，连续失败才进入 5 秒退让，避免牺牲祝福、圣疗术等动作卡死或重复投递。
- 修复低压填充顺序：审判发送前最终校验敌对目标存在且不超过 28 码；圣光术兜底只有在神圣震击无充能、审判可用且无灌注时才允许，避免在其他技能可用时大量裸读圣光术。
- 修复吸奶盾缺少真实结束锚点时的误判：成功事件没有 `cast.endsAt` 且没有严格 DiGua 姓名板回退锚点时只记录 `missing_end_anchor` 并保持安全状态，不伪造阶段 3 时间。
- 默认移除 DiGua 姓名板监测和 AOE 状态机向 WoW 聊天框输出的 `[Fuyutsui AOE]` 调试提示；实时监测和 Shigure 应用诊断日志保持不变，需要排查时仍可用 `/fu aoedebug` 显式开启。
- 固化吸奶盾美德的双输入实现与回归门禁：真实施法按实际结束时点加 2 秒进入阶段 3；受保护环境由兼容桥复刻 DiGua 实时姓名板条件，建立 11.7 秒倒计时并在结束 2 秒后回退进入阶段 3。新增权威说明和契约断言，防止误改回兼容桥自身 addon table 的无效钩子。
- 修复敌方可读 `UNIT_SPELLCAST_START` 先于 DiGua 时间轴事件到达时读条永久失配的问题；Fuyutsui 现在把同姓名板、同 Spell ID 的读条候选保留 0.5 秒，时间轴随后到达即可完成绑定。
- 吸奶盾保留可用的 `UNIT_SPELLCAST` 读条路径，同时以 DiGua 实时姓名板倒计时作为受保护字段环境下的可靠入口；普通 AOE 仍只由可信真实读条进入执行窗口。
- 修复普通 `UNIT_SPELLCAST_STOP` 被诊断为读条失败的问题；`STOP` 现在只表示终态未知并返回资源预留，只有明确中断、失败或死亡才增加失败诊断。
- 新技能首次投递也会等待 GCD 进入 50ms 队列窗口，避免在 GCD 中段提前发送、确认超时后再次发送；圣疗术、光环掌握和紧急动作保持即时响应。等待日志不再混入其他决策的按键、目标和规则编号。
- 修复大秘境中敌方读条 Spell ID、敌对状态或数值时间受保护时 AOE 状态机完全收不到候选读条的问题；可读值仍严格匹配 Spell ID，受保护值只接受与新 DiGua 预警在 0.5 秒内关联的姓名板读条，并由 App 解码 DurationObject 剩余时间决定美德窗口。
- AOE 诊断增加原始读条、Spell ID 受保护、敌对状态受保护和受保护匹配计数；App 在启动、扫描失败和 `/reload` 后先静默重建计数基线，不再输出 `+100`、`+255` 等协议错位假数据。
- 玩家施法事件的 Spell ID 受保护时改为发布匿名动作确认；只有一个待确认动作时 App 才消费该确认，减少技能已经释放却因状态不可读而重复发键的问题。
- 修复 DiGua 桥接在职业状态色块建立前写入握手、导致内存已就绪但像素永久保持 0 的初始化顺序问题；玩家色块建立后现在会重新发布桥接状态。
- 修复旧版“烈日奶骑大秘境-美德爆发 12.1”因模块 ID 迁移被错误保留的问题；仅对已知精确 SHA-256 的 1.2.1.9 文件先备份再升级，使无 900ms 全局暂停和吸奶盾预留保护的新版规则真正生效。
- 修复固定 250ms 重投递导致技能慢、按键淹没和读条治疗重复发送的问题；运行时现在消费 WoW 玩家施法事件，GCD 中未接受的动作只在真实队列窗口补发一次。
- 修复待确认门控范围过大、审判和清洁术确认失败时阻塞新出现治疗需求的问题；当前最高优先级治疗可立即抢占普通待确认动作，旧确认同时取消，较低优先级技能仍不能覆盖治疗队列。
- 修复治疗吸收预警未匹配真实读条时长期停留在资源预留、继而把已经出现的真实团伤美德阻断到事件结束的问题；反应式美德现在只看实时实际生命缺口，不计治疗吸收，也不沿用 6 秒旧爆发保持。
- 修复地瓜时间轴事件派发时字段受保护后兼容桥无法补入 Spell ID、导致美德始终停在资源预留的问题；桥接现在从地瓜调用时间轴 API 的原始输入重发，并在事件 ID 可用后恢复取消联动。
- 修复战斗状态像素最多延迟 1 秒更新，造成开战动作慢和脱战后继续施法的问题；进出战事件现在当帧刷新，进攻不再被不稳定的姓名板敌人数硬门槛阻断。
- 修复脱战灌注转换反复投递圣光术；规则要求真实灌注层数且当前未读条，玩家施法 START 后立即停止重复发送。
- 修复桥接作为新插件目录时必须先运行 WoW 才能同步、却又必须在 WoW 启动前存在的循环依赖；桥接实现现由既有 Fuyutsui 本体加载，更新后执行一次 `/reload` 即可握手，握手缺失或关键时间轴 API 不可用时运行时仍不发键。
- 修复高优先级普通 GCD 技能抢占待确认动作时绕过真实队列窗口、导致中段无效投递和临窗重复投递的问题；同技能同目标的规则升级现在只更新实时优先级，目标变化仍可在队列窗口替换。

### 新增

- Fuyutsui 增加固定两字段的 AOE 预警协议：普通 AOE 由非零 Spell ID 的可信敌方读条进入执行窗口；吸奶盾优先按真实读条实际结束时点进入，并可由 DiGua 实时姓名板倒计时在后置延迟结束后回退进入阶段 3。
- 增加随 Fuyutsui 加载的 DiGua 兼容桥；它按关键时间轴 API/数据能力为标准 AOE 预警重发携带真实读条 Spell ID 的时间轴事件，并在兼容目录复刻吸奶盾姓名板触发入口，不修改或分发 DiGua 文件。检测到的版本只用于诊断，不作为硬编码门禁。
- 增加受控 AOE 调试日志，可用 `/fu aoedebug` 记录事件 ID、Spell ID、施法者、施法实例和结束原因。

### 改进

- 烈日奶骑模块升级至 1.2.1.21 并随应用交付；吸奶盾阶段 3 可由明确成功终态或 DiGua 姓名板倒计时完成后的 2 秒后置延迟产生。健康队伍先审判，审判不可用或圣能已满才使用无美德黎明之光。首次启动只补装缺失模块，精确匹配已知缺陷内置版本时先备份再升级，其他同 ID 或同名用户模块保持不变。该条为历史版本记录，当前模块为 1.2.1.22。
- 美德爆发改为显式窗口约束：圣洁鸣钟、光环掌握和爆发型黎明之光只在美德光环生效时使用；当前无美德黎明之光已采用严格 H95 条件，旧版全员至少 96% 的口径仅保留作历史记录。
- DiGua 资源预留与最后安全 GCD 期间，实时群伤规则不再提前消耗美德；普通 AOE 阶段 2 和吸奶盾阶段 3 继续作为可信预铺时机，伤害生效尾窗仍保留反应式兜底。实时群伤资格保持 2 秒，避免首个治疗 GCD 后瞬时缺口消失而中断美德爆发链。
- 玩家动作确认改为先发布技能码与状态码、最后发布动作序号，避免 App 截到新序号配旧动作的中间帧并把未生效的 DiGua 美德误判为成功。
- 普通群疗改为至少 3 人治疗负荷达到 10% 且总负荷达到 45%，两人低于 70% 且总负荷达到 50% 或两人不高于 50% 也会触发；触发后保持 6 秒爆发链。
- 真实多人群伤且圣能足够时，美德后优先使用黎明之光；只有治疗吸收而没有真实群伤时仍使用永恒之火处理最大吸收目标。
- 鸣钟和光环掌握前改用永恒之火清圣能；低压输出仍按正义盾击、进攻神圣震击、审判排序。
- 神圣圣骑士的进攻宏优先对队伍坦克的当前目标施放；坦克目标暂时不可用时依次回退到当前目标的目标和当前敌对目标，避免审判失败退让后直接裸读圣光术。
- 色块识别异常时会在屏幕中央持续提示，支持原生全屏与所有 Space，扫描恢复后显示恢复通知；浮动条同时显示具体原因。
- “有效性”复用原有色块携带死亡、坐骑、聊天、饮水等暂停原因，界面加载未完成时也会在屏幕中央明确提示。
- 脱战且全队满血时，即将到期的圣光灌注会在圣能未满时转化为圣能。
- 奶骑 AOE 预警使用插件按当前急速计算的实际 GCD 判断最后安全施法窗口；运行时同时读取真实 GCD 剩余值控制唯一一次队列窗口补发。
- AOE 资源预留与等待吸收期间，全员至少 96% 且无治疗吸收时继续按正义盾击、进攻神圣震击、审判输出；仅最后安全 GCD 窗口暂停非紧急技能。
- 屏幕中央状态提示仅在配置的 WoW 进程位于 macOS 前台时显示，切换到其他应用会立即隐藏；浮动状态条保持原行为。
- 低压治疗细分为两层震击、灌注闪现和最后一层震击三档；93% 至 95% 且有两层震击时改用规则 26 裸读圣光闪现，已有灌注时自然消耗；审判按灌注的 `+2` 圣能预测避免抢占治疗增益和溢出。
- 光环掌握仅在至少 3 人存在明显缺口且总治疗负荷达到 45 时使用，避免消耗在群疗爆发末尾的单人或双人缺口。

### 修复

- 修复 macOS 本地重打包后屏幕录制和辅助功能显示“未授权”的问题；固定外层/UI 与运行时子进程的签名主体，恢复 `Shigure.MacApp` 的兼容标识，并在打包时拒绝证书或 designated requirement 漂移。
- 修复圣盾术施加自律后，圣疗术规则反复抢占优先级并阻断后续治疗的问题。
- 修复队伍 `HARMFUL` 自律槽忽略法术过滤、把任意有害光环误报为自律的问题；运行时清除旧协议上报的队友误值，只保留可确认的玩家自身自律，队友圣疗资格交由 WoW 校验并以冷却变化确认成功。
- 治疗吸收改为连续两帧确认正值，过滤单帧噪声的同时保留 1% 至 100% 的精确吸收值。
- 修复普通 AOE 仅凭 DiGua 倒计时提前释放美德的问题；现在必须匹配对应真实读条。
- 修复吸奶盾时点不稳定的问题；普通时间轴或 `STOP` 不会单独提交事件。可读取真实施法时以 `1306517` 的实际 `cast.endsAt + 2 秒`进入阶段 3，真实时点不可用时才由严格姓名板条件建立的 DiGua 11.7 秒倒计时在额外等待 2 秒后回退；治疗吸收光环 `1306550` 仅用于后续吸收处理。
- 修复相同 Spell ID 的并发预警和施法互相串扰，以及时间轴未知移除、施法者死亡和迟到终态可能保留错误执行窗口的问题。
- 修复美德后的首个永恒之火降低缺口人数后，鸣钟因瞬时阈值消失而中断的问题。
- 美德、光环掌握、圣洁鸣钟、清洁术、读条治疗和圣能技能统一通过玩家施法事件确认；冷却、充能、圣能与神圣意志变化保留为兼容兜底，不同普通技能不得覆盖等待队列。
- 圣光闪现和圣光术在玩家施法 START 后立即确认，当前读条结束前不再重复选择和发键；圣疗等更高优先级动作仍可立即抢占。
- 修复美德连续未生效并退让后，圣洁鸣钟和光环掌握仍被美德光环/冷却条件锁死的问题；群疗链现在可按实际可用技能降级执行。
- 奶骑清洁术改为选择插件已解码的任意可驱散类型，覆盖魔法、疾病和中毒；日志增加可驱散槽位及目标驱散类型。
- 动作投递成功后立即发布运行日志，不再因下一帧规则限流而丢失清洁术等技能的投递结果。
- 修复 Shigure 先于 WoW 启动时插件同步被永久跳过的问题；启动运行时前会重新同步，有更新时要求 `/reload` 后再运行，避免新应用驱动旧插件。
- 修复神圣专精识别成功但没有匹配模块时退回“C# 职业逻辑尚未迁移”、导致技能无法释放的问题。
- 修复旧版单键 keymap 被保留后与 `selector-target-v1` WoW 宏混用、导致日志显示已投递但技能未释放的问题；运行配置和键位现在始终从活动 Fuyutsui 权威源重新生成。
- 恢复清洁术优先级：有人低于 70% 时先急救，全员至少 70% 时在 AOE 爆发链之后、所有普通治疗之前驱散；无效动作由统一的两轮确认失败退让处理。
- 修复奶骑模块用单位 0 查询美德道标、而选择器路由只为玩家单位 1 生成绑定，导致美德条件命中后仍被静默跳过的问题。
- 修复战斗状态下高频监控刷新或扫描提示变化时，已显示的状态窗口被重复显示、运行时 UI 回调异常逃出 Avalonia 主循环并导致应用 `SIGABRT` 的问题；失败视图现在独立停更并写入持久诊断，不影响施法运行时。
- 修复 AOE 第 18 条暂停规则在资源预留和等待吸收期间抢占满血进攻分支、导致审判、进攻神圣震击和正义盾击均不执行的问题。
- 修复满血进攻仍要求治疗者当前选中敌对目标的问题；进攻神圣震击和审判改为坦克目标，正义盾击不再读取当前目标类型与距离。
- 修复同一技能连续切换目标时，确认超时被错误归因到首次目标并触发 5 秒目标级退让的问题；多目标窗口超时不再累计单目标失败。
- 修复 96% 至 99% 微小血量缺口把进攻分支全部挡住、导致队伍健康时长期发呆的问题。
- 修复吸奶盾阶段 1 的无关群体缺口提前消耗美德的问题；阶段 1 保留美德，真实读条按实际结束时点加 2 秒、严格 DiGua 姓名板倒计时按结束后 2 秒进入阶段 3。
- 修复全员健康时神圣意志没有被正义盾击消费、反而被无美德黎明之光或等待自动消失的问题；免费正义盾击现在先于健康群加和输出填充。

### 验证

- 最新验证为 Release 全量构建 0 警告/0 错误、80 项契约测试通过，以及实际加载生产 Lua 的 AOE 生命周期回放通过。
- Apple Silicon 与 Intel 候选均完成构建、架构检查和本地深度签名校验；当前本地候选为 `Shigure-1.2.1.22-fixes-arm64.app` 与 `Shigure-1.2.1.22-fixes-x64.app`，仅供本机实战测试，最新吸奶盾时点仍待游戏内日志验收。

## 1.2.1.7 - 2026-08-30

### 新增

- 增加 `selector-target-v1` 安全单位路由：治疗技能复用 30 个目标键，降低大型团队模块的宏槽位占用。
- 增加独立治疗吸收像素协议、真实生命值校正、治疗缺口选择器和去重诊断日志。
- 增加友方非玩家目标支持，模块可在队伍安全时治疗当前 NPC。
- 增加模块宏路由依赖保存、导入冲突检查以及对应的生成 keymap。

### 改进

- 治疗、驱散、人数和吸收统计会排除死亡、不可协助、超距或不在视野的队伍槽位。
- 玩家生命值使用独立状态与队伍位置校正，减少组队像素滞后导致的错误目标选择。
- 有序按键序列会在发送前完整校验，并在技能选择键与目标键之间保留稳定间隔。
- 运行日志改为虚拟化逐行显示并限制为最近 2000 行，降低长时间运行后的界面卡顿。
- 触发输入逐个消费离散脉冲，并在事件监听异常退出后自动恢复。

### 修复

- 修复清洁术等定向技能选中错误队伍成员后重复尝试、阻断后续治疗的问题。
- 修复圣疗术受瞬时错帧影响而对满血目标释放的问题；危急目标现在必须连续两帧满足条件。
- 修复应用重启后单实例接管失败、模块加载错误信息不足及部分运行状态等待问题。
- 修复 Retina 缩放下治疗吸收像素错位以及吸收值覆盖真实生命值的问题。

### 验证

- Release 全量构建通过，契约测试 66 项全部通过。
- Apple Silicon 与 Intel 目标构建通过，本地应用包签名验证通过。
